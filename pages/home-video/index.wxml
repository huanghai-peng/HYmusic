<!--pages/home-video/index.wxml-->
<view class="video">
  <view wx:for="{{mvTop}}" wx:key="id" class="item">
      <video-item-v1 item="{{item}}" bindtap="handleVideoItemClick" data-id="{{item.id}}" />
  </view>
</view>